# React Native / Metro cannot follow pnpm's symlinked, strictly-isolated
# node_modules: transitive runtime deps (@babel/runtime, whatwg-fetch,
# react-native-css-interop, …) must be resolvable from the app directory.
# Hoisted linking is the documented pnpm setup for RN monorepos.
node-linker=hoisted
